#include "main.h"
#include <stdio.h>
/**
 * *_strpbrk - this function checks if its parameter is lower case or uper case
 * @s: the parameter in form of an int
 * @accept: the parameter in form of an int
 * Return: 1 if lowercase and 0 if something else
 */

char *_strpbrk(char *s, char *accept)
{
	return (s);
}
